import { UiVariantTransitionRegistry } from './registry';

/** Shared registry used by route workspaces and the root variant controller. */
export const uiVariantTransitionRegistry = new UiVariantTransitionRegistry();
