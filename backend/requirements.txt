fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.37
asyncpg==0.30.0
psycopg[binary]==3.2.13
aiosqlite==0.20.0
alembic==1.14.1
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
cryptography==44.0.0
httpx==0.28.1
orjson==3.10.15
Pillow==11.1.0
pillow-avif-plugin==1.5.2
pillow-heif==0.21.0
openpyxl==3.1.5
reportlab==4.2.5
pytest==8.3.4
pytest-asyncio==0.25.2
zxing-cpp==3.1.1
# Kimlik OCR yerel motoru (0.3.39): RapidOCR PP-OCRv6 det/rec modelleri wheel'in
# İÇİNDE gelir (rapidocr/models/*.onnx) — runtime indirme yok. onnxruntime
# rapidocr 3.9.2'nin TEMEL bağımlılığı DEĞİL (extra da tanımlamıyor) — motorun
# çalışması için açık pin şart; eksikse engine_available() sessizce False döner.
# DİKKAT (cv2 çakışması): rapidocr opencv-python (GUI derlemesi) sert bağımlılığı çeker.
# opencv-python-headless ile birlikte kurulunca ikisi aynı cv2/ dizinine yazar ve
# hangisi SON kurulduysa o kazanır (pip kurulum sırasını garanti ETMEZ).
# Kurulumdan SONRA şu düzeltme koşmalı (build-windows-runtime.ps1 otomatik yapıyor,
# dev kurulumlarında da elle çalıştırın):
#   pip uninstall -y opencv-python opencv-python-headless
#   pip install --no-deps opencv-python-headless==4.11.0.86
rapidocr==3.9.2
onnxruntime==1.29.0
opencv-python-headless==4.11.0.86
numpy==2.5.3
