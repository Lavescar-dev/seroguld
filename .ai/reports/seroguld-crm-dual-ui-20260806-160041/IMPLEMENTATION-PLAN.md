# Implementation Plan

1. Record branch/runtime/worktree baseline and inspect the handoff archive.
2. Add the isolated variant controller, storage, guard registry, fallback boundary, and tests.
3. Build the modern design system, shell, page presentations, primary modules, and typed adapters.
4. Wire existing hooks/APIs to both renderers without changing route meaning.
5. Close the public-display raw identity leak and add a regression test.
6. Add minimum artifact metadata and bounded Tauri diagnostics.
7. Run frontend, backend, and Rust validation; request a security/correctness review.
8. Produce gap/matrix reports and rotate verified archives.
